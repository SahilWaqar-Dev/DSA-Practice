# Linked List

Linked list is a data structure that holds a collection of nodes where each node consist of two things one is the data and other is the address of the next node We will have the reference of the first node and by using it we will reach to the last node It is dynamic in size. Linked list has many types like singly doubly circular etc.

## Singly Linked List

here is the code of singly linked list that i just practiced. In this code there is a complete implementation of singly linked list It includes the following methods

isempty
insertatstart
deleteatstart
insertatend
deleteatend
traverse
search


 But in this implementation,insertatend and deleteatend have the time complexity of O(n)