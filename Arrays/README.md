# Array Practice Codes

these are the simple code of arrays that i just practice. 

## What is in these files:
- in these files i built the array in different ways 
- access the elements of the array in different ways 
- also I try to learn the memory management that how these elements are stored in array and address calculations 
- also accessing the n index address of the array element at n index 

so simple these files are just about this